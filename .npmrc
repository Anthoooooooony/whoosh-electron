# electron-builder 期望传统 node_modules 结构（不依赖 pnpm symlink 解析 native addon）
# 详见 BLUEPRINT.md H-4
node-linker=hoisted

# 严格 engine 校验
engine-strict=true

# CI 拒绝 lockfile 不一致
prefer-frozen-lockfile=true

# Electron 二进制走国内镜像（如果在国内拉取慢，按需打开）
# electron_mirror=https://npmmirror.com/mirrors/electron/
