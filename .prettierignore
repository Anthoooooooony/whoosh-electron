node_modules
dist
out
release
.vite
pnpm-lock.yaml
src/native/prebuilds
src/native/**/build

# Vendored mattpocock skill bundles + skills-lock.json：入仓但作为外部资产不格式化
.claude
.agents
skills-lock.json

# 归档的设计/规划文档手工排版，prettier 会破坏 markdown 缩进与代码块
archive

# release-please 生成的 changelog，不接受手工格式化
CHANGELOG.md

# README 资产（SVG placeholder + 后续替换的截图），prettier 不格式化二进制/手绘 SVG
readme-assets
